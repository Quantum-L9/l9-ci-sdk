# Pinned runtime dependency versions for reproducible CI installs.
# Install with: pip install -e . -c constraints.txt
# Regenerate by resolving the abstract ranges in pyproject.toml and recording
# the exact versions validated in CI.
jsonschema==4.26.0
referencing==0.37.0
PyYAML==6.0.1
