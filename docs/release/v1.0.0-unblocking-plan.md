<!-- L9_META
l9_schema: 1
repo: l9-ci-sdk
path: docs/release/v1.0.0-unblocking-plan.md
layer: docs
owner: platform
status: active
version: 1.0.0
updated: 2026-07-31
/L9_META -->

# v1.0.0 unblocking + doc-drift resolution plan

**Audit base:** `origin/main` @ `64bd142c6728b7180f3ff20bbc0f12728636b9af` (2026-07-31)  
**Companion canvas:** Cursor canvas `v1-unblocking-plan.canvas.tsx`  
**Authority order:** `.l9/*` and ADRs win over AGENTS/README prose (ALIGNMENT.md).

---

## 0. Decision fork

| Path | Goal | Semgrep lifecycle | When to choose |
|---|---|---|---|
| **A — SDK contract freeze** | Tag/publish `l9-ci==1.0.0` as analysis-contract SDK | Remains `experimental` | Default critical path of this plan |
| **B — Provider GA** | Semgrep `supported` + roadmap GA criteria | `experimental` → `shadow` → `supported` | Only after Path A; separate program (W6) |

**Do not tag v1.0.0 claiming Semgrep supported/GA.** Path A release notes must state experimental + link `docs/release/known-limitations.md`.

---

## 1. Workstream map

| WS | Name | Unblocks | Depends on | Exit criterion |
|---|---|---|---|---|
| W0 | Repo hygiene | Safe editing | — | Clean worktree on intended tip; `make check` green |
| W1 | Evidence seal | REL-001 | W0 | Zero `{{PLACEHOLDER}}` in seal set **or** dated waiver table (no silent deletes / fake URLs) |
| W2 | Doc drift packs | DOC-001..003 | W0 (parallelizable with W1 drafts) | AGENTS / ALIGNMENT / VALIDATION / known-limitations match `.l9` + code |
| W3 | Checklist truthing | Release honesty | W2 | Path A items `[x]` with verify cites; Path B items remain `[ ]` and labeled |
| W4 | Tag + GitHub Release + PyPI | REL-003 | W1–W3 | `v1.0.0` tag, Release, `publish.yml` outcome understood |
| W5 | Post-tag verify | Confidence | W4 | Install smoke + CI on tagged SHA |
| W6 | Path B (P4→supported) | REL-002 GA | W5 | `release-policy` supported + shadow observation evidence |

---

## 2. W0 — Repo hygiene (preflight)

1. `git fetch origin && git status -sb` on the operator clone.  
   - Commit or discard local noise (`.cursor*`, `.vscode`) before branch work.  
   - Never switch/stash over valuable WIP (AGENTS §12).
2. Prefer an isolated worktree:  
   `git worktree add ../l9-ci-sdk-unblock origin/main`
3. Confirm tip SHA (today’s audit tip or a later docs-only tip after merges).
4. Toolchain: `make bootstrap` (or off-Dropbox `PYTHON=` / `PRE_COMMIT_BIN=`).  
   **Gate:** `make check` exit 0 before first edit.
5. Single Path A branch: `docs/v1-release-unblock` (no extra parking branches).

---

## 3. W1 — Evidence seal (REL-001)

### 3.1 Seal file set (must end with zero unresolved placeholders)

- `.l9/audit-findings.md`
- `docs/release/blocker-closure.md`
- `.l9/roadmap.yaml` (P4 `evidence:`)
- `.l9/release-policy.yaml` (Semgrep `evidence:`)

**Ledger law:** audit-findings header forbids treating the ledger as sealed while placeholders remain.  
**blocker-closure law:** “No evidence URL in this document is a placeholder” must become literally true.

### 3.2 Placeholder registry (13 keys)

| Placeholder | Approx count | Resolution method | Owner |
|---|---:|---|---|
| `SDK_S1_SHA` | 11 | 40-char commit for runtime contract (public API / CLI / version policy) from PR #17 series or successor | agent + human confirm |
| `SDK_CODE_SHA` | 5 | Commit that landed runtime Semgrep fixture + full-path test (QA-010); cross-check `tests/fixtures/semgrep/runtime/provenance.yaml` | agent |
| `SDK_WORKFLOW_SHA` | 4 | Commit that landed thin `l9-analysis*.yml` callers + required self-validation CI | agent |
| `CORE_ANALYZE_SHA` | 5 | **Live pin today:** `c3f04e1268364e3623fc57f963937e2a0665e0e0` (all five callers identical) | agent |
| `CORE_ACTIONS_SHA` | 2 | Historical composite-action pin **or** `waived: superseded by thin analyze-semgrep caller` + date | human if ambiguous |
| `CORE_PUBLISH_SHA` | 2 | Core publish pin at gate-publication proof time **or** waived if publication is inside reusable analyze workflow | human + Core |
| `CORE_GATE_PUBLICATION_RUN_URL` | 5 | Actions run URL for Core gate/publish dogfood | human (`gh run list`) |
| `CORE_GATE_CHECK_URL` | 5 | Permalink to published GitHub check | human |
| `AUD_008_ISSUE_URL` | 3 | Admin issue for required-check ruleset | human/admin |
| `AUD_008_RULESET_URL` | 5 | Org ruleset URL making SDK self-validation required on `main` | human/admin |
| `AUD_008_NEGATIVE_PROOF_URL` | 2 | PR blocked when required check failed | human/admin |
| `AUD_008_POSITIVE_PROOF_URL` | 3 | PR merged after required check passed | human/admin |
| `SDK_VALIDATION_ARTIFACT_URL` | 5 | Commit-bound self-validation artifact from `ci.yml` evidence step on release tip | agent + human confirm |

### 3.3 Procedure

1. **Create** `docs/release/evidence-map.yaml` as the single map:

   ```yaml
   # schema sketch
   entries:
     SDK_S1_SHA:
       value: "<40-char sha>|null"
       status: resolved|waived|blocked_admin
       reason: "..."
       resolved_at: "YYYY-MM-DD"
       approver: "..."   # required if waived
   ```

2. Render values into all four seal files from that map (no hand-divergent edits).
3. For `blocked_admin` / unavailable AUD-008 proofs: Path A may proceed **only** with explicit waiver rows (status `waived`, reason, date, approver). **Never invent URLs.**
4. **Gate command:**

   ```bash
   rg '\{\{[A-Z0-9_]+\}\}' \
     .l9/audit-findings.md \
     docs/release/blocker-closure.md \
     .l9/roadmap.yaml \
     .l9/release-policy.yaml
   # expect: no matches
   ```

5. Re-read blocker-closure “Required assertions” and tick only what evidence supports.

---

## 4. W2 — Doc drift resolution packs

### Pack D1 — `AGENTS.md` §3 (index must match `.l9/roadmap.yaml`)

| Location | Current (stale) | Replace with | SSOT |
|---|---|---|---|
| P1 status | `complete_with_fixture_limitation` | `complete` — runtime fixture + provenance present | `.l9/roadmap.yaml` P1 |
| P3 status | `current` | `complete` | `.l9/roadmap.yaml` P3 |
| P4 status | `blocked` (needs fixture + live Core) | `ready_for_promotion`; `blockers: []` — observation not yet claimed | `.l9/roadmap.yaml` P4 |
| Semgrep blurb | experimental → eventual shadow | Keep **experimental**; note P4 ready_for_promotion, observation not started | `.l9/release-policy.yaml` |

Retain the “ignore stale Phase-1 bans” warning; ensure it does not reintroduce P4=blocked.

### Pack D2 — `ALIGNMENT.md` “Known alignment gaps”

| Gap | Action | Detail |
|---|---|---|
| #1 MANIFEST / VALIDATION_REPORT | **Rewrite** | `MANIFEST.md` is live (317 files) via `l9-ci manifest`; `VALIDATION_REPORT.json` is absent — mark historical/absent, not current seal |
| #2 issue #9 / no pyproject | **Delete/replace** | `pyproject.toml` exists; issue #9 **CLOSED**; document dual install paths (`requirements.txt` + `pyproject.toml`) as aligned |
| #3 issue #5 action pins | **Delete/replace** | issue #5 **CLOSED**; SHA pins enforced by `lint/check_action_pins.py` |
| #4 @v1 vs SHA pin | **Keep** | Intentional repo-specific SHA authority |
| #5 arch rev vs SDK 1.0.0 | **Keep** | Distinct version axes; keep labeled |

### Pack D3 — `VALIDATION.md`

1. Evidence table: drop/mark `VALIDATION_REPORT.json` absent; `MANIFEST.md` = current inventory (cite file count + manifest CLI).  
2. Live surfaces: keep; add pointer to `make check` as local gate SSOT.  
3. Local regeneration: replace ad-hoc ruff/mypy/pytest with Makefile targets from AGENTS §10.  
4. Baseline / freshness: runtime fixture exists; remaining reseal gap is **W1 evidence placeholders**, not missing `generate_manifest`.  
5. Bump L9_META `updated` date.

### Pack D4 — `docs/release/known-limitations.md`

| Section | Action | Correct claim |
|---|---|---|
| Semgrep fixture | **Rewrite** | Runtime fixture at `tests/fixtures/semgrep/runtime/`; representative fixtures remain for unit edges; **supported** promotion still Path B |
| Semgrep version range | **Rewrite** | Enforced `>=1.100.0,<2.0.0` via `SemgrepVersionPolicy`; callers pin `1.171.0` |
| Coverage | Keep (tighten) | Limited when `paths.scanned` absent |
| Policy format | Keep | Core governance YAML not assumed compatible |
| Autofix | Keep | Explicit `remediation_class` only |
| Execution isolation | Keep | Timeout/output bounds; no OS sandbox |
| Network controls | **Delete/rewrite** | `network_allowed` **removed** (DWA-008); network is caller/runtime concern |

### Pack D5 — incidental (same PR if cheap)

- `.l9/public-api.yaml` header comment “11 public packages” → align with architecture `public_surface` (12 incl. `rulesets`) **or** keep explicit “rulesets is package-data / separate check” wording without the wrong count.  
- `docs/PUBLISHING.md`: list exact runtime pins (jsonschema/referencing/PyYAML); restate Trusted Publisher prerequisite before tag.  
- `README.md`: **only** if this PR owns install/release surface change (docs ownership §13); otherwise link out from release notes — no feature-marketing append.

---

## 5. W3 — Checklist truthing (`docs/release/checklist.md`)

| Section | Mark `[x]` when | Verify | Path A / B |
|---|---|---|---|
| Contracts | Schema + semantic + agent + compat green | `make check` / compatibility + artifact tests | A |
| Fixture provenance | Runtime `results.json` + provenance fields | `tests/providers/semgrep/test_runtime_fixture.py` **not skipped** | A |
| Security / Determinism / Operational | Covered by existing suites | Footnote each `[x]` with test module paths; add tests before checking | A |
| Integration: Core example | Manual dry run of `docs/examples/core-semgrep-integration.sh` | Record date/operator in checklist | A (or dated waiver) |
| Integration: shadow upload | Shadow path proven | Core/SDK dogfood artifact | **B** — leave `[ ]` |
| Promotion | Policy promotion approved | release-policy + observation | **B** — leave `[ ]` |

**Must edit** the HTML comment under Fixture provenance: harness no longer “SKIPS until captured” when fixture is present.

---

## 6. W4 — Tag, GitHub Release, PyPI

1. Confirm PyPI Trusted Publisher: project `l9-ci`, repo `Quantum-L9/l9-ci-sdk`, workflow `publish.yml`, environment `pypi` (`docs/PUBLISHING.md`).  
2. `workflow_dispatch` `publish.yml` (build + twine check only) — must succeed.  
3. Draft notes: contract freeze; Semgrep **experimental**; link known-limitations; changelog since `v0.1.0`; **non-claims** list (no supported/GA, no P4 observation complete, no fabricated evidence URLs).  
4. Merge Path A PRs; wait for green CI on merge commit.  
5. Annotated tag `v1.0.0` at that SHA; push tag (**never** force).  
6. `gh release create v1.0.0 --target <sha> --notes-file …`  
7. Observe `publish.yml` on tag: success **or** fail-closed on missing OIDC — fix publisher and re-run; do not fake publish.

---

## 7. W5 — Post-tag verification

- `pip install l9-ci==1.0.0` (or git+sha) → `l9-ci providers list` → Semgrep `experimental`.  
- Version triad still `1.0.0`.  
- Dogfood: analysis merge + self-validation green on tagged commit.  
- `l9-ci manifest check --tracked-only --exclude-dir memory-bank`.

---

## 8. W6 — Path B outline (not Path A merge gate)

1. Shadow observation window with real Core gate publication/check URLs (from W1).  
2. Promote `.l9/release-policy.yaml`: `experimental` → `shadow` → `supported` (STACK-010).  
3. Complete checklist **Promotion** section; update AGENTS Semgrep lifecycle; roadmap P4 → complete; review GA criteria.  
4. **Do not** start P5 second provider until Semgrep `supported` and bundle has no known breaking defects.

---

## 9. Recommended PR slicing (Path A)

| PR | Scope | Why | Order |
|---|---|---|---|
| **A1** | W1: `evidence-map.yaml` + seal file renders | Evidence honesty reviewable alone | 1 |
| **A2** | W2 packs D1–D5 + W3 checklist | Doc-only realignment | 2 (after A1) |
| **A3** (optional) | Release notes draft + PUBLISHING touch-ups | Keep A2 small | 2 or 3 |
| **Tag ops** | W4–W5 | No code PR — tag + `gh release` | last |

Ship via `make push` on each PR. No `--no-verify`.

---

## 10. Path A acceptance matrix

| Check | Proof |
|---|---|
| No unresolved placeholders in seal set | `rg` empty (or waivers only, no `{{…}}`) |
| `make check` green | local gate |
| AGENTS phases match roadmap | table diff |
| known-limitations match code | version policy present; no `network_allowed` |
| Checklist Path A items `[x]` | file + test cites |
| `providers list` → experimental | CLI |
| `v1.0.0` tag + GitHub Release | `git tag` / `gh release view` |
| Release notes include non-claims | human read |

---

## 11. Explicit non-goals

- Second provider (P5)  
- Claiming Semgrep `supported` / GA in v1.0.0 notes  
- Inventing AUD-008 admin URLs  
- Force-push / history rewrite  
- README feature-marketing append  
- Core repo changes  
- Weakening zizmor / local push gate  
- Skipping `make check`

---

## 12. Immediate next actions (human choose one)

1. **Approve Path A** and authorize implementation starting at W0→W1 (agent fills resolvable SHAs; human supplies AUD-008 / Core run URLs or waivers).  
2. **Pause tag** until admin AUD-008 proofs exist (still do W2/W3 doc drift in parallel).  
3. **Expand to Path B** only after Path A tag — do not conflate in one PR.
