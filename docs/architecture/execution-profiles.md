# Execution Profiles

Execution profiles separate native-only, import-only, and supported-provider execution. Provider selection combines the registry, detected capabilities, profile rules, installation state, and deterministic provider ordering.
